/*
var c = 1;
while(c <= 5) {
    
}

do {
    console.log(c);
    c++;
} while(c <= 5);

*/

/*
    Using structure for to run code in loops.
    - 'for' - Need declaration of the variable and also
      declaration of the increment using incremental
      operators. Also need the condition to when it will
      runs the code block.
*/

console.log('Starting...');
for(var c = 1; c <= 6; c++) {
    console.log(c);
}
console.log('Ending...');
